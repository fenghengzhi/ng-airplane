import { Url2varnamePipe } from './url2varname.pipe';

describe('Url2varnamePipe', () => {
  it('create an instance', () => {
    const pipe = new Url2varnamePipe();
    expect(pipe).toBeTruthy();
  });
});
